{
  "mcpServers": {
    "google-sheets": {
      "command": "uvx",
      "args": ["mcp-google-sheets@latest"],
      "env": {
        "CREDENTIALS_PATH": "${GOOGLE_OAUTH_CREDENTIALS:-${HOME}/.claude/unit-economics/credentials.json}",
        "TOKEN_PATH": "${HOME}/.claude/unit-economics/token.json"
      }
    }
  }
}
